============
Installation
============

At the command line::

    $ easy_install django-like-lookup

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-like-lookup
    $ pip install django-like-lookup
